# Agent Memory Backup Changelog

Empty in this template. Add an entry each time you re-export per the versioning policy in `README.md`, e.g.:

## 0.1.0 — YYYY-MM-DD
- Initial backup: `<file>.md` added, covering `<what it's about>`.
