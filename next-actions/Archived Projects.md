Projects tagged `status/done` and moved out of `002 projects/` via the "📦 Archive project" button — kept for reference, out of the way of active work.

```dataview
LIST del
FROM "005 archive"
SORT file.name ASC
```
