Projects tagged `focus/daily` — you've marked these as mattering enough that they should get attention every day, regardless of due dates or where they'd otherwise rank. Always included in the daily check shortlist.

```dataview
TABLE del AS "Deliverable"
FROM "002 projects"
WHERE contains(tags, "focus/daily")
```
