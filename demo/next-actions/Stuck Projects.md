Same data as [[Waiting For]], grouped by project instead of flat — the projects, not the tasks, are the headline here.

```tasks
not done
path includes projects
tags include #task-category/waiting-for
group by filename
sort by due
```
